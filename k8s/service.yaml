apiVersion: v1
kind: Service
metadata:
  name: k8s-operator
  namespace: default
spec:
  type: NodePort
  selector:
    app: k8s-operator
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007